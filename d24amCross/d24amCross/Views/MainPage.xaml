<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:d24amCross"
             x:Class="d24amCross.MainPage">

  <Grid>
    <ListView x:Name="FeedList" HasUnevenRows="True">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell >

            <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" Padding="10">
              <Image Source="{Binding Imagem}" Aspect="AspectFill" />
              <Label Text="{Binding Titulo}" HorizontalTextAlignment="Center" VerticalTextAlignment="End" FontSize="18"/>
            </StackLayout>

          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>

</ContentPage>